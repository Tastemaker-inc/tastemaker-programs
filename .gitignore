.anchor
.DS_Store
dist
target
**/*.rs.bk
node_modules
test-ledger
.env
*.log
.validator-test.log
*.json
!Anchor.toml
!package.json
!package-lock.json
!tsconfig.json
!**/idl/*.json
!**/idl/**/*.json
# Keep deployments/*.json for recorded program IDs; ignore keypairs
*keypair*.json
id.json
deploy-keypair.json

# Internal team runbooks are local-only (not pushed publicly)
docs/

# Metaplex program binary for local test validator (CI downloads via run-test-full.sh)
test-programs/*.so
